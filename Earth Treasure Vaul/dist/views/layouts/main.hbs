<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/static/styles/styles.css" />
    <title>Home Page</title>
</head>

<body>

    {{!-- <div>
        <div class="errorContainer">
            <p>Error</p>
        </div>
    </div>  --}}

    <div id="wrapper">
        <header>
            <nav>
                <div>
                    <a href="{{SERVER_ENDPOINTS.home}}">Home</a>
                    <a href="{{SERVER_ENDPOINTS.dashboard}}">Dashboard</a>
                    <a href="{{SERVER_ENDPOINTS.search}}">Search</a>
                </div>

                {{#if isAuthenticated}}
                    <div class="user">
                        <a href="{{SERVER_ENDPOINTS.create}}">Add Stones</a>
                        <a href="{{SERVER_ENDPOINTS.logout}}">Logout</a>
                    </div>
                {{else}}
                    <div class="guest">
                        <a href="{{SERVER_ENDPOINTS.login}}">Login</a>
                        <a href="{{SERVER_ENDPOINTS.register}}">Register</a>
                    </div>
                {{/if}}
            </nav>
        </header>

        {{{body}}}

        <footer>
            <p>@EarthTreasureVaul</p>
        </footer>
    </div>
</body>

</html>